bash
cd frontend
Install Dependencies (if needed) If this is your first time running it on a new setup, install the node modules:

bash
npm install
Start the Development Server Run the following command to start Vite:

bash
npm run dev
Open in Browser Onc